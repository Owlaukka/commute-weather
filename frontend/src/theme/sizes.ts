export interface ThemeSizes {
  commuteTimeFormHeight: number;
  sliderThumbSize: { value: number; unit: string };
}

const sizes: ThemeSizes = {
  commuteTimeFormHeight: 3.5,
  sliderThumbSize: { value: 1, unit: 'rem' },
};

export default sizes;
